# Why agent logs are not enough

普通 agent 只告诉你“它跑完了”，但说不清它本来想做什么、有没有经过策略检查、有没有验证执行真实性、有没有导出可交接收据。这个最小样品把同一套本地评测结果压成一页，直接对比 no-evidence run 和 evidence-chain run 的差别。

Data source: `artifacts/metrics/comparison-summary.json`.

## Baseline 缺什么

1. intent not explicitly captured
2. policy not explicitly checked
3. execution verification not performed
4. receipt not exported with verifiable artifact digests

Observed in `16/16` baseline runs. Receipt digest count range: `0-0`.

## Evidence chain 多了什么

1. intent captured
2. policy checked
3. execution verified or explicitly blocked/tamper-detected
4. bounded receipt exported

Observed in `16/16` evidence-chain runs. Outcome mix: `pass=11`, `policy_blocked=3`, `tamper_detected=2`.

## Three minimal cases

### A. completed

Task `task-001`: **Summarize architecture path**. Both runs finish, but only the evidence-chain run leaves explicit intent, policy, verification, and receipt evidence.

| run | result_status | overall_status | intent | policy | verification | receipt digests | bundle |
| --- | --- | --- | --- | --- | --- | ---: | --- |
| baseline | `completed` | `partial` | `false` | `false` | `not_performed` | 0 | `artifacts/runs/task-001/baseline` |
| evidence_chain | `completed` | `pass` | `true` | `true` | `verified` | 4 | `artifacts/runs/task-001/evidence_chain` |

### B. policy_blocked

Task `task-010`: **Budget overflow review**.

- Result: `blocked_budget` with review status `policy_blocked`.
- Reason: Budget policy would be exceeded.
- Receipt: `4` digests exported from `artifacts/runs/task-010/evidence_chain`.
- Takeaway: The run is blocked before completion, but the policy decision and bounded receipt still make the stop independently reviewable.

### C. tamper_detected

Task `task-015`: **Detect action digest mismatch**.

- Result: `tamper_detected` with verification `tamper_detected`.
- Receipt: `4` digests exported from `artifacts/runs/task-015/evidence_chain`.
- Review summary: Integrity verification independently corroborated a tamper condition.
- Takeaway: The evidence chain does not just log a failure. It records a digest-backed receipt showing that tamper was detected.

## Run locally

```bash
bash scripts/run_demo.sh
make killer-demo
python3 -m http.server --directory docs 8000
```

_Generated by `scripts/export_minimal_killer_demo.py`._
