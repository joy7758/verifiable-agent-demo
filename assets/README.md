# README Screenshot Placeholders

This directory is reserved for real demo screenshots and GIFs. Do not add placeholder images.

Current README asset:

- `audit-receipt.png` — output from `python3 -m demo.agent` showing generated audit evidence artifacts

Follow-up captures:

- `demo-run.gif` — short terminal recording of `python3 examples/enterprise_sandbox_demo/run.py`
- `artifact-chain.png` — view of `intent.json -> policy.json -> trace.jsonl -> sep.bundle.json -> replay_verdict.json -> audit_receipt.json`

Keep the captures simple: command, generated files, validation or replay result, and receipt.
